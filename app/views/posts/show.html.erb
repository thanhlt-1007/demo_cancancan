<div class="container">
  <h2><%= @post.title %></h2>

  <table class="table">
    <tbody>
      <tr>
        <th>User</th>
        <td><%= @post.user.email %></td>
      </tr>
      <tr>
        <th>Content</th>
        <td><%= @post.content %></td>
      </tr>
    </tbody>
  </table>
</div>
